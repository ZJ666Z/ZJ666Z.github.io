# Portfolio QA Checklist

每次还原/修改页面时，按下面的同类问题逐页检查。每项都要和
`/__orig/...`（Framer 原版导出）在同一视口下对比，不能只凭印象。

## 1. 页面结构与间距

- [ ] Framer 原版同一张大白色 section 里的内容，本地没有拆成多个带 96px 顶距的小 section。
- [ ] 每个 section 的 padding/gap 与 Framer 实测一致（常见 32/36/64/128）。
- [ ] 标题、卡片、段落之间的垂直间距没有明显大/小 20px 以上。
- [ ] 整页 body 高度与原版接近（误差优先控制在 ±8px）。
- [ ] 大宽图在 Framer 里属于 section 外独立全屏区时，本地没有把它继续塞在 section 里。
- [ ] 页面底部到 footer、footer 卡片到版权行的距离与原版一致。

## 2. 文本与排版

- [ ] “Brief / Focus / Role”这类标签在原版是 18px 加粗正文段时，不用成 24px h3。
- [ ] 小标题、说明文字的左/中/右对齐和原版一致。
- [ ] 重要产出数据（数量、百分比、金额）在原版加粗时，本地同样加粗。
- [ ] 列表/段落语义和原版一致（不要用 `<br>` 拼多个 `<p>`，不要该用 `<ul>` 却用 `<p>`）。
- [ ] 拼写没有 typo（例如 Pre-purchase 不是 Pre-pruchase）。

## 3. 卡片与标签

- [ ] 卡片圆角、内边距、背景色与原版一致（24px 卡片、36px 大卡、32px 大面板等）。
- [ ] Framer 里每列是一整张等高卡时，本地没有拆成多张矮卡。
- [ ] 同一行/列的卡片高度相等，不因内容长度不同而一高一矮。
- [ ] pill/胶囊：白色标签用浅灰细描边；深色 outline 标签用 2px；文字居中或左对齐跟原版。
- [ ] 编号圆点统一为 48px，编号文字 24/32；圆点与后面标签的排版和原版一致（同行 vs 换行）。
- [ ] 左右两栏之间的箭头/连接元素位置和宽度正确。
- [ ] 头像/Logo 圆描边统一（粗细一致），重叠 logo 有底部背景不透视。

## 4. 图片

- [ ] 截图类大图不要被圆角裁掉内容；只有 Framer 原版确实带圆角的图（如 16px auth 图）才保留圆角。
- [ ] 图片宽高比、显示高度与原版一致，没有压扁/拉伸。
- [ ] 横向全景图高度是完整视口高度，且能水平滚动；验证时不仅看截图，还要滚动到底部/最右。
- [ ] 图注位置正确：原版 caption 在图上/图下/图前时本地一致。
- [ ] 带 emoji 的 caption 不要因行高/剪裁卡在 `opacity:.001`，滚动到后必须可见。
- [ ] 使用原始高清图，不做 CSS 强行放大。

## 5. 动效与交互

- [ ] reveal 元素滚动进入后都能触发，不会停在隐藏态（尤其白色 clip section 的最后一个元素）。
- [ ] 动画位移/时长与 Framer 的 fade-up 96px + spring 一致，没有把整块又改成直线。
- [ ] 鼠标/滚轮提示图标有持续动画。
- [ ] 导航 hover 圆点、移动端菜单 hover、当前页指示状态正确。
- [ ] 项目卡 hover 图片缩放/箭头出现等状态齐全。
- [ ] marquee/logo 跑马灯滚到视口后才开始，且循环连续。
- [ ] `prefers-reduced-motion` 下这些装饰动画会停，内容不隐藏。

## 6. 响应式

- [ ] 390 / 834 / 1280 / 1440 下 `scrollWidth <= clientWidth`。
- [ ] 卡片、pill、标题文字没有溢出或相互遮挡。
- [ ] 横向滚动图只在全屏 panorama 上出现，不影响正常页面宽度。
- [ ] 桌面规则在 max-width 断点里都有对应回退（固定列宽/固定高度要在小屏释放）。

## 7. 最后再整体过一遍

- [ ] `git diff --check` 无空白错误。
- [ ] 本地服务重新打开后，对照 Framer 快速滚一遍所有 section。
- [ ] 每完成一轮修改都推送 GitHub。
